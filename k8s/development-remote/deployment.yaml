apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-monappc-monappc-frontend-deployment
  namespace: dev-monappc-monappc-frontend
  labels:
    app: dev-monappc-monappc-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-monappc-monappc-frontend
  template:
    metadata:
      labels:
        app: dev-monappc-monappc-frontend
    spec:
      containers:
        - name: dev-monappc-monappc-frontend
          image: corentin35/monappc-monappc-frontend:develop-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
