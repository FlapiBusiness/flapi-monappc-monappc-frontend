apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-monappc-monappc-frontend-ingress
  namespace: dev-monappc-monappc-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - dev.monappc.flapi.org
      secretName: dev-monappc-monappc-frontend-tls
  rules:
    - host: dev.monappc.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: dev-monappc-monappc-frontend-service
                port:
                  number: 3000
