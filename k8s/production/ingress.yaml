apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-monappc-monappc-frontend-ingress
  namespace: prod-monappc-monappc-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monappc.flapi.org
      secretName: prod-monappc-monappc-frontend-tls
  rules:
    - host: monappc.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-monappc-monappc-frontend-service
                port:
                  number: 3000
