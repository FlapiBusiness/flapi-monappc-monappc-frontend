apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-monappc-monappc-frontend-deployment
  namespace: staging-monappc-monappc-frontend
  labels:
    app: staging-monappc-monappc-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: staging-monappc-monappc-frontend
  template:
    metadata:
      labels:
        app: staging-monappc-monappc-frontend
    spec:
      containers:
        - name: staging-monappc-monappc-frontend
          image: corentin35/monappc-monappc-frontend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
