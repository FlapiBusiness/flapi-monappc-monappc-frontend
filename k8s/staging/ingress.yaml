apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-monappc-monappc-frontend-ingress
  namespace: staging-monappc-monappc-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.monappc.flapi.org
      secretName: staging-monappc-monappc-frontend-tls
  rules:
    - host: staging.monappc.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-monappc-monappc-frontend-service
                port:
                  number: 3000
