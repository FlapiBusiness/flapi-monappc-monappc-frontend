apiVersion: v1
kind: Service
metadata:
  name: staging-monappc-monappc-frontend-service
  namespace: staging-monappc-monappc-frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: staging-monappc-monappc-frontend
